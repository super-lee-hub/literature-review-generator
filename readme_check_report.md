# README.md 准确性检查报告

## ✅ 总体评估

README.md 整体结构清晰，但存在一些与代码实际实现不一致或遗漏的地方。

## ⚠️ 发现的问题

### 1. 命令行参数缺失

**README.md 中未提及的参数：**

- `--analyze-only`: 仅运行阶段一（分析文献并生成摘要）
- `--config`: 指定配置文件路径（默认为 config.ini）
- `--merge`: 合并额外的 summaries.json 文件

**位置：** `main.py` 第 3478-3527 行

### 2. 概念增强模式参数说明不完整

**README.md 中提到概念增强模式，但未详细说明参数：**

- `--prime-with-folder`: 指定种子论文文件夹路径
- `--concept`: 指定要学习的概念名称

**位置：** `main.py` 第 3524-3525 行

### 3. 输出文件说明不完整

**README.md 中未提及的输出文件：**

- `[项目名称]_literature_review_outline.md`: 文献综述大纲（Markdown 格式）
- `[项目名称]_review_checkpoint.json`: 综述生成断点文件
- `[项目名称]_concept_profile.json`: 概念学习笔记（概念增强模式）
- `[项目名称]_zotero_report_for_retry.txt`: Zotero 模式重跑报告

**位置：** `main.py` 第 389, 996, 1018, 1541 等行

### 4. 配置说明重复且混乱

**问题：**

- "高级配置与 API 设置" 部分已经提供了 `.env` 和 `config.ini` 的说明
- 后面又重复出现了 "安全配置（重要！）"、"交互式配置（推荐）"、"手动配置（高级选项）" 三个章节
- 内容重复，造成混淆

**建议：** 保留前面的折叠式配置说明，删除后面的重复章节。

### 5. 进阶功能与实际参数不一致

**README.md 中的进阶功能：**

```bash
# 自动重试上一次失败的文献
python main.py --project-name "消费者行为研究" --retry-failed
```

**实际代码中的参数：**

```bash
python main.py --project-name "消费者行为研究" --retry-failed
```

✅ 这部分是正确的。

### 6. 常见问题回答不够全面

**缺少的常见问题：**

- 如何处理扫描版 PDF？
- API 速率限制如何处理？
- 输出文件格式可以自定义吗？
- 如何获取 Zotero 报告？（虽然提到了但没有详细步骤）

### 7. 故障排除部分重复

**问题：**

- 文档中有两处故障排除：一处是简化的 FAQ，另一处是详细的故障排除
- 两处内容有重叠，建议合并或删除其中一处

### 8. 项目结构说明位置不当

**问题：**

- 项目结构说明位于文档末尾，普通用户不需要关心
- 建议将其移入折叠的"开发者指南"中

## 📋 建议修改清单

### 高优先级

1. **添加缺失的命令行参数说明**
   - 在"进阶功能"或单独章节添加 `--analyze-only`, `--config`, `--merge` 的说明

2. **完善概念增强模式说明**
   - 补充 `--prime-with-folder` 和 `--concept` 参数的使用示例

3. **完善输出文件说明**
   - 在"你将获得什么"或"输出文件说明"章节补充缺失的文件类型

4. **删除重复的配置章节**
   - 保留前面的折叠配置说明，删除后面的重复内容（第 132-208 行）

### 中优先级

5. **合并故障排除章节**
   - 将两处故障排除合并为一处，放在 FAQ 之后

6. **移动项目结构说明**
   - 将项目结构说明移入"开发者指南"折叠区域

### 低优先级

7. **补充常见问题**
   - 添加关于扫描版 PDF、API 限制、输出格式等常见问题

8. **优化排版**
   - 统一标题层级和格式
   - 检查链接是否有效（如 DEVELOPER_GUIDE.md）

## 🔍 验证方法

要验证这些发现，可以运行以下命令：

```bash
# 查看所有支持的命令行参数
python main.py --help

# 查看配置文件示例
cat config.ini.example

# 查看 .env 示例
cat .env.example

# 运行测试验证功能
pytest tests/ -v
```

## ✅ 正确的部分

- 核心功能描述准确
- 快速开始流程基本正确
- Zotero 模式和 PDF 模式的使用方法正确
- API 配置方法正确
- 断点续传功能描述正确
- 输出文件的主要类型（.docx, .xlsx, .json）正确
