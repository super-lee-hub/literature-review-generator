# Source requirements for the production runtime.
# Release installations use the generated Windows/Python 3.11 lock:
# requirements-py311-windows-prod.lock

# Core runtime
pandas
openpyxl
python-docx
pdfplumber
PyMuPDF
pymupdf4llm
requests
tqdm
python-dotenv
typing_extensions

# GUI
nicegui

# Advanced PDF rescue. Optional local-RAG dependencies live in
# requirements-optional-rag.txt and are excluded from the release lock.
docling
