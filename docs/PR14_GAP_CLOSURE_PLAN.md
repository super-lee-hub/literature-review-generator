# PR14 Gap Closure Plan

Audit date: 2026-08-03 (Asia/Shanghai)

Repository: `super-lee-hub/literature-review-generator`

Branch: `codex/platform-hardening-outline-v3`

Base: `origin/main` at `ecac15976ebb3b6ee754fe5c0dfe44efacd72e9a`

Reviewed head: `2158faa1d392acdc2250d147b26ed8ca367203d6` (baseline audit snapshot)

This is a fact audit at the current PR head. It is intentionally separate
from implementation work. The audit used the current production call graph
and targeted `git grep` searches; file existence and isolated unit tests were
not treated as integration evidence.

## Audit method

The primary relation checks were run from the repository root:

```text
git grep -n -E "InternalStageExecutorRegistry|_execute_analyze|_execute_review|OutlineV3Executor|persist_review_chain|ProviderRuntime|PersistentQueueService|list_job_runtimes|OutlineAdoptionTransaction|run_summary_rechecks|run_repair_apply" -- runtime services validation outline gui reviewctl.py
git grep -n -E "status.*stable|adopt_outline|candidate_1|healthy if|_runtimes|values\(\)" -- runtime services validation outline gui reviewctl.py
```

The current worktree contains exactly two unrelated untracked user files,
`PPH_五份综述_20260731.zip` and `PPH_完整资料包_20260731.zip`; neither is in
this audit or in the Git index.

## Gap matrix

Status meanings: `IMPLEMENTED_ONLY` means a component exists without a
verified production caller; `INTEGRATED` means a caller exists but the
required current-architecture E2E proof is missing; `E2E_VERIFIED` is reserved
for a completed end-to-end test; `REGRESSED` means the current caller violates
the target contract; `BLOCKED` means the production path cannot perform the
required operation; `NOT_IMPLEMENTED` means no current production path was
found.

| Requirement | Current implementation | Production caller | E2E evidence | Status | Required action |
| --- | --- | --- | --- | --- | --- |
| Raw PDF Stage 1 generation | `_execute_analyze()` only loads canonical summaries and raises when none exist (`runtime/orchestrator.py:384-404`) | `InternalStageExecutorRegistry.execute()` dispatches `analyze` (`runtime/orchestrator.py:591-595`) | No current raw-PDF generation test | BLOCKED | Add a current Stage 1 service that preprocesses, builds input, calls Reader, validates schema, and persists closure |
| Zotero Stage 1 generation | Same summary-only branch; Zotero source intake does not reach a Reader generation service | `AgentRuntimeBridge.build_source_bundle()` (`runtime/orchestrator.py:629-679`) | No current Zotero generation E2E | BLOCKED | Route verified Zotero `SourceBundle` items through the same Stage 1 service |
| Stage 1 multimodal input | Builders and visual artifact modules exist | No caller from `_execute_analyze()` | No provider request assertion on the current path | IMPLEMENTED_ONLY | Invoke `Stage1InputBuilder`, visual selection, capability fallback, and receipt metadata from Stage 1 service |
| Summary reuse | `_load_summary_payloads()` and identity validation exist (`runtime/orchestrator.py:300-379`) | Analyze/derived actions consume explicit summary sources | Existing bridge tests cover reuse contracts only | INTEGRATED | Keep reuse as a separate mode and add mixed generation/reuse E2E |
| Outline global evidence construction | v3 evidence, ledger, and matrix are built (`outline/v3_executor.py:395-410`) | `_execute_outline()` constructs `OutlineV3Executor` (`runtime/orchestrator.py:437-486`) | No full current-chain E2E | INTEGRATED | Bind every downstream prompt to the canonical global evidence artifact and prove it with provider fakes |
| Relation adjudication | Relations are confirmed by confidence/paper-count filtering (`outline/v3_executor.py:416-424`) | `OutlineV3Executor.run()` | No semantic adjudication E2E | REGRESSED | Separate deterministic candidates from configured model adjudication and persist an honest method/provider record |
| Candidate generation | Provider request contains paper keys, relation IDs, and hashes (`outline/v3_executor.py:444-455`) | Candidate provider nodes in `OutlineV3Executor.run()` | No evidence-content assertion | REGRESSED | Pass compact corpus evidence, intent, contract, constructs, mechanisms, findings, contradictions, and gaps |
| Critiques and arbitration | Provider nodes exist, but invalid candidate IDs fall back to the first candidate (`outline/v3_executor.py:458-467`) | `OutlineV3Executor.run()` | No malformed-arbitration E2E | REGRESSED | Fail closed on invalid selection and persist a retryable invalid receipt |
| Section evidence packets | Packet semantic fields are all empty (`outline/v3_executor.py:473-494`) | `section_evidence_packets` node is generated by the outline executor | No non-empty packet E2E | REGRESSED | Populate field-level evidence and provenance from the evidence view and summaries |
| Coverage audit | Only a limited paper/must-use/claim/relation shape is checked (`outline/v3_executor.py:500-505`) | Outline executor creates `coverage_audit` | No full coverage E2E | REGRESSED | Add claim, contradiction, gap, method, context, duplicate, unsupported, and empty-section checks |
| Stability audit | Status and all perturbation checks are hard-coded stable (`outline/v3_executor.py:506-509`) | Outline executor creates `stability_audit` | No metamorphic test | REGRESSED | Run real order/shard/replay/hash perturbations and compare semantic structure |
| Explicit adoption | Runtime defaults `adopt_outline` to true and executor directly creates adoption (`runtime/orchestrator.py:464-485`; `outline/v3_executor.py:514-520`) | `_execute_outline()` | No pre-adoption review-block test | REGRESSED | Stop at `adoption_eligible` by default and use only `OutlineAdoptionTransaction` for explicit adoption |
| Outline replay/resume | Durable node store and replay primitives exist (`outline/v3_executor.py:353-359`) | Outline executor loads completed nodes | No binding-change replay E2E | IMPLEMENTED_ONLY | Include all required replay bindings and invalidate on content/model/config/prompt changes |
| Writer review generation | `_execute_review()` joins final Outline claims into section content (`runtime/orchestrator.py:526-577`) | Internal `review` stage dispatch (`runtime/orchestrator.py:591-595`) | No Writer call test | BLOCKED | Add durable section Writer executor consuming evidence packets and structured citation output |
| Citation ref catalog | Catalog-aware draft builder exists (`runtime/orchestrator.py:215-247`) | Called by `persist_review_chain()` | No real Writer-generated citation data | INTEGRATED | Build/register document and section catalogs before Writer calls and reject unknown refs |
| Review draft v3 | Current persistence calls `build_review_draft()` (`runtime/orchestrator.py:1028-1035`) | `persist_review_chain()` | No review-generation E2E | INTEGRATED | Make Writer-produced `review_draft_v3` the truth source rather than Outline projection |
| Citation manifest v3 | Manifest is persisted after draft (`runtime/orchestrator.py:1036-1044`) | `persist_review_chain()` | No non-empty occurrence E2E | INTEGRATED | Generate manifest from structured Writer blocks and require real occurrences |
| DOCX rendering | DOCX projection is rebuilt from draft and manifest (`runtime/orchestrator.py:1045-1053`) | `persist_review_chain()` | No full chain render/readback E2E | INTEGRATED | Keep DOCX projection-only and add unresolved-token/readback assertions |
| Citation-based bibliography | DOCX writer and manifest contain cited-entry logic | Called only after the current claim-only draft | No cited-subset E2E | IMPLEMENTED_ONLY | Prove cited-only bibliography from Writer tokens and exclude uncited papers |
| Review semantic validation | Built-in validator is called through the runtime adapter (`runtime/orchestrator.py:1113-1129`) | `validate` stage | No current full review E2E | INTEGRATED | Validate claim units against source text/visual refs and reject zero-claim normal reviews |
| Summary recheck | Current service extracts anchors and rule conflicts (`validation/summary_recheck.py:163-260`) | `run_summary_rechecks()` is available to validation/repair | No source-grounded AI recheck E2E | REGRESSED | Add targeted provider recheck with source chunks/pages, visual refs, and canonical field ownership |
| Repair plan | Control plane exposes repair planning and current planner models | `reviewctl repair-plan` | Existing unit/integration tests only | INTEGRATED | Bind the planner to complete Registry dependency hashes and current outline artifact types |
| Repair apply | `RepairApplier` mutates in-memory draft/manifest and emits recheck trigger strings (`validation/repair_apply.py:438-495`) | `reviewctl repair-apply` | No immutable derived-artifact E2E | REGRESSED | Produce quarantined versioned artifacts, perform mapping-first changes, and never overwrite READY inputs |
| Targeted revalidation | Current apply path records strings such as `summary_recheck:<id>` (`validation/repair_apply.py:491-495`) | Repair apply | No real targeted validation E2E | REGRESSED | Execute affected claim, summary-field, manifest, DOCX, and dependency checks as canonical results |
| Queue GUI | Persistent queue returns a list (`services/queue_service.py:266`), while GUI calls `.values()` (`gui/app.py:2170-2173`); worker also writes private state | GUI queue page and `QueueRunner` | Existing queue unit tests only | REGRESSED | Establish one public runtime contract and remove direct `_runtimes`/`_save()` writes |
| Queue CLI | `reviewctl` parser has no queue subcommands (`reviewctl.py:37-64`) | None | No CLI queue tests | NOT_IMPLEMENTED | Add JSON queue-add/list/run/retry/cancel/remove/export/import over the same service |
| Cancellation | Control plane persists cooperative cancellation requests (`runtime/control_plane.py:730-762`) | `reviewctl cancel` and runtime cancellation store | No full multi-stage cancellation E2E | INTEGRATED | Add boundary checks for preprocess/provider/register/section/validation/repair/export |
| Completion evaluation | Provider completion evaluator exists, but Outline health uses `not self.diagnostics` and fixed audits | Runtime and Outline executors | No closure-complete E2E | REGRESSED | Make one canonical evaluator verify required nodes, receipts, hashes, schema, dependencies, and finish reasons |
| Provider receipts | Outline provider calls use `ProviderRuntime`; Stage 1 and Writer have no real provider caller | Outline executor only | No all-node receipt closure E2E | INTEGRATED | Bind receipts to every current provider node and verify the exact call graph |
| Export bundle | Control plane invokes `ExportBundleService` (`runtime/control_plane.py:816-825`) | `reviewctl export` | No complete product-chain export E2E | INTEGRATED | Require closure allowlist, payload-byte checksums, ZIP verification, atomic rename, and Registry registration |
| Forensic attestation | `reviewctl attest` and control-plane attestation surfaces exist | `reviewctl.py:60-63`, control plane | No full-chain attestation E2E | INTEGRATED | Attest actual final hashes, receipts, dependency graph, and completion evidence |
| End-to-end workflow | No `tests/test_current_runtime_full_e2e.py` or equivalent current full-chain test exists | No verified end-to-end caller | Required test file absent | NOT_IMPLEMENTED | Add 3-paper raw-PDF current-architecture E2E and make every provider call/evidence/adoption/export assertion real |

## Audit conclusion

This matrix is the pre-implementation baseline captured at the reviewed head;
its `REGRESSED`, `BLOCKED`, and `NOT_IMPLEMENTED` entries are retained as the
audit record rather than silently rewritten. Current implementation and test
evidence is recorded in `PR14_CURRENT_VALIDATION_EVIDENCE.md`. The supplied
completion definition still requires the final compile, type, collection,
strict-marker, diff, remote-branch, Draft-PR, and CI checks before completion
can be claimed.
