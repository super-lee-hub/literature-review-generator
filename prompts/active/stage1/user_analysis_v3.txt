请根据下面的论文证据生成 summary_v2_lite canonical JSON。

【任务顺序】

1. 判断论文类型及分类置信度。
2. 提取研究问题、理论基础、研究设计、数据来源、样本、变量、分析方法、主要发现、结论、贡献和作者明确陈述的局限。
3. 根据论文类型填写对应 specialized_details。
4. 检查标题、作者、年份、期刊、DOI 等元数据；只能使用证据中明确出现或请求中明确提供的值。
5. 比较正文、OCR、表格、图注和视觉观察是否存在冲突。
6. 完成 quality_audit。
7. 按唯一输出契约返回 JSON。

【全文与预处理证据】

{{PAPER_FULL_TEXT}}

【视觉覆盖与输入完整性】

{{VISUAL_COVERAGE_JSON}}

【唯一输出契约】

{{SUMMARY_SCHEMA_CONTRACT}}

【强制规则】

- 上面的输出契约由当前代码中的 summary_schema.py 动态生成，必须严格遵守。
- 找不到信息时使用契约允许的 null 或空数组。
- 不得用“大约”“可能”“预计”“通常”“可推断”为缺失事实补值。
- 不得生成示例数据。
- 不得把参考文献列表内容当作本文结果。
- 不得把图中看不清的数字写入 findings。
- 不得把统计相关、预测关系或组间差异改写成因果关系。
- 不得忽略 quality_audit。
- 只返回 JSON。

代码必须从 summary_schema.py 动态构建 SUMMARY_SCHEMA_CONTRACT，不得在 Prompt 中另外维护一份会漂移的手写 schema。契约至少包含默认结构、允许枚举、论文类型分支字段和 null/empty 规则。
