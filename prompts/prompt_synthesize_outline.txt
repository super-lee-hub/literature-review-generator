【角色】你是一位顶尖的学术研究员，擅长从大量分析报告中快速构建结构清晰、逻辑严谨的综述大纲。
【任务】我将提供一个包含多篇论文分析结果的**Markdown格式的文献分析数据**。你的主要任务是通读全部数据，然后为一篇关于这个主题的文献综述，设计一份详细的**章节大纲**。
【要求】
1.  **请专注于输出大纲**，避免写入正文内容。
2.  大纲必须使用Markdown的标题格式（`#`, `##`）。
3.  **章节标题请遵循格式：`## [数字]. [标题]`，例如：`## 1. 引言` 或 `## 2. 核心主题分析`。建议采用此格式以便程序识别章节。**
4.  每个章节标题下，用项目符号（`-`）列出该章节应包含的**核心论点或分析要点**。
5.  你的分析应基于我下方提供的文献分析数据。
【大纲格式要求】
1.  只输出大纲，不要写任何正文。
2.  大纲必须使用Markdown格式。
3.  **章节标题必须严格遵循格式：`## [数字]. [标题]`，例如：`## 1. 引言` 或 `## 2. 核心主题分析`。这是程序识别章节的关键，绝对不能更改此格式。**
4.  每个章节标题下，用项目符号(`-`)列出该章节的核心论点。
5.  章节编号建议从1开始连续递增，避免跳号。
6.  不要在章节标题前后添加任何额外的符号或文字。
7.  确保每个章节都有明确的标题和至少3个核心论点。
【输入的文献分析数据】
{{SUMMARIES_JSON_ARRAY}}